import 'package:flutter/material.dart';

class SupportWelcome extends StatelessWidget {
  const SupportWelcome({super.key, required this.title});
  final String title;

  @override
  Widget build(BuildContext context) {
    throw UnimplementedError();
  }
}